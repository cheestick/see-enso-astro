---

---
<section class="hero">
  <h1 class="visually-hidden">SeeEnso - Digital Agency</h1>
  <picture class="hero__picture">
    <source
      type="image/webp"
      srcset="/images/hero/hero-bg.webp 1x,
              /images/hero/hero-bg@2x.webp 2x,
              /images/hero/hero-bg@3x.webp 3x," >
    <source
      type="image/jpeg"
      srcset="" >
    <img src="/images/hero/hero-bg.jpg" alt="" width="1920" height="1080">
  </picture>
  <img class="logo-main" src="/images/hero/see-enso-da-hero.svg" alt="">
  <div class="deco">
    <img style="--delay: 750ms;" src="/images/hero/halo-l-1.svg" alt="">
    <img style="--delay: 650ms;" src="/images/hero/halo-l-2.svg" alt="">
    <img style="--delay: 550ms;" src="/images/hero/halo-l-3.svg" alt="">
    <img style="--delay: 450ms;" src="/images/hero/halo-l-4.svg" alt="">
    <img style="--delay: 350ms;" src="/images/hero/halo-l-5.svg" alt="">
    <img style="--delay: 250ms;" src="/images/hero/halo-l-6.svg" alt="">
  </div>
</section>

<style is:global>
  .hero {
    display: grid;
    grid-template-areas: 'hero';
    place-items: center;
    width: 100%;
    max-width: 100%;
    height: 100vh;
    height: 100dvh;
    /* overflow: hidden; */
  }

  .hero__picture {
    width: 100%;
    height: 100%;
    grid-area: hero;
  }

  .hero__picture img {
    width: 100%;
    min-height: 100%;
    object-fit: cover;
    object-position: center center;
  }

  .logo-main {
    grid-area: hero;
    width: min(90vw, 320px);
  }

  @property --s-factor {
    syntax: "<number>";
    inherits: false;
    initial-value: 1;
  }

  .deco {
    --deco-scale: 1;
    grid-area: hero;
    display: grid;
    grid-template-areas: deco;
    transform-origin: center;
    transform: 
              translateY(50px)
              scale(var(--deco-scale));
  }

  .deco > img {
    grid-area: deco;
    /* max-width: 100%; */
    transform: scale(var(--s-factor));
    animation: decoScale 2.2s infinite ease alternate;
    animation-delay: var(--delay, 0s);
  }

@media screen and (width > 40em ) {
  .deco {
    --deco-scale: 1;
  }
}

@keyframes decoScale {
  to {
    --s-factor: 1.1;
  }
}
</style>